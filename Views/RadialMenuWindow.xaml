<Window x:Class="QuickWheel.Views.RadialMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:QuickWheel.Controls"
        mc:Ignorable="d"
        Title="QuickWheel Radial Menu"
        Width="400"
        Height="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Deactivated="Window_Deactivated"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        KeyDown="Window_KeyDown"
        KeyUp="Window_KeyUp">
    <Grid x:Name="MenuGrid" Background="Transparent">
        <controls:CircularPanel x:Name="SectorPanel"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>

        <Ellipse x:Name="CenterCircle"
                 Width="60"
                 Height="60"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 StrokeThickness="0"
                 Fill="Transparent"/>

        <TextBlock x:Name="CenterText"
                   Text="QuickWheel"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="{Binding CenterTextSize}"
                   FontWeight="Bold"
                   IsHitTestVisible="False">
            <TextBlock.Foreground>
                <SolidColorBrush x:Name="CenterTextBrush" Color="White"/>
            </TextBlock.Foreground>
        </TextBlock>
    </Grid>
</Window>
